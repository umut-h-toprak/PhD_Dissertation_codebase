export class VariantVizTypeEntry {
    constructor(data){
        this.variantVizTypeIndex=data.variantTypeIndex;
        this.variantDesc=data.variantDesc;
        this.colourId=parseInt(data.colourId);
    }
}